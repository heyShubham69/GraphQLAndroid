query Country($country_code : ID!) {
    country(code: $country_code) {
        code
        name
        capital
        emoji
        currency
        phone
        languages {
            name
        }
        continent {
            name
        }
    }
}
